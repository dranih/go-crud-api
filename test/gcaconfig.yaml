api:
  driver: "sqlite"
  address: "../../test/test.db"
  database: "test"
  tables: "sharks"

  middlewares:
  - basicAuth:
      - mode: "required"
      - realm: "GoCrudApi : Username and password required"
      - passwordFile: "../../test/test.pwd"
  - json:
