api:
  driver: "sqlite"
  address: "../../test/test.db"
  database: "test"
  tables: "sharks"

  middlewares:
  - basicAuth:
      - mode: "optional"
      - realm: "Username and password optionals"
      - passwordFile: $HOME/Work/go-crud-api/test/test.pwd
  - json:
