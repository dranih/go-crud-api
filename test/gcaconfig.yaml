api:
  driver: "sqlite"
  address: "../../test/test.db"
  database: "test"
  tables: "sharks"

  middlewares:
  - dbAuth:
      - mode: "optional"
      - realm: "Username and password optionals"
  - json:
